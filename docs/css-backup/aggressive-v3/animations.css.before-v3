/*
 * Sistema de movimento premium — refinamento V6
 * ------------------------------------------------
 * Princípios usados nesta versão:
 * - movimento curto e progressivo, sem salto inicial;
 * - gatilho mais tarde, próximo da área real de leitura;
 * - textos sem blur para preservar nitidez e desempenho;
 * - imagens com escala quase imperceptível;
 * - interações sem mola, giro ou excesso de elasticidade.
 */

.reveal {
  --reveal-delay: 90ms;
  --reveal-duration: 1320ms;
  --reveal-distance: 18px;
  --reveal-blur: 0px;
  --reveal-ease: cubic-bezier(.45, 0, .25, 1);

  opacity: 1;
  transform: none;
  filter: none;
}

.js .reveal {
  opacity: 0;
  will-change: opacity, transform;
  transition:
    opacity calc(var(--reveal-duration) * .82) var(--reveal-ease) var(--reveal-delay),
    transform var(--reveal-duration) var(--reveal-ease) var(--reveal-delay);
}

.js .reveal--up {
  transform: translate3d(0, var(--reveal-distance), 0);
}

.js .reveal--left {
  transform: translate3d(calc(-1 * var(--reveal-distance)), 0, 0);
}

.js .reveal--right {
  transform: translate3d(var(--reveal-distance), 0, 0);
}

.js .reveal--scale {
  transform: translate3d(0, 12px, 0) scale(.992);
}

.js .reveal--fade {
  transform: translate3d(0, 7px, 0);
}

.js .reveal--visible {
  opacity: 1;
  transform: translate3d(0, 0, 0) scale(1);
  will-change: auto;
}

/* A linha da consulta é desenhada com calma após a entrada do bloco. */
.js .process-steps.reveal::before {
  transform: scaleX(0);
  transform-origin: left center;
  transition: transform 1550ms cubic-bezier(.45, 0, .25, 1) 360ms;
}

.js .process-steps.reveal--visible::before {
  transform: scaleX(1);
}

/* Entrada inicial do hero. Mantém a composição original e apenas suaviza o ritmo. */
.hero-intro {
  opacity: 0;
  animation: hero-intro-in 1380ms cubic-bezier(.45, 0, .25, 1) forwards;
}

.hero-intro--title {
  animation-delay: 120ms;
  transform: translate3d(0, 22px, 0);
}

.hero-intro--text {
  animation-delay: 300ms;
  transform: translate3d(0, 17px, 0);
}

.hero-intro--trust {
  animation-delay: 470ms;
  transform: translate3d(0, 14px, 0);
}

.hero-intro--action {
  animation-delay: 620ms;
  transform: translate3d(0, 12px, 0);
}

.hero-intro--doctor {
  animation-delay: 280ms;
  transform: translate3d(20px, 0, 0) scale(.992);
}

@keyframes hero-intro-in {
  to {
    opacity: 1;
    transform: translate3d(0, 0, 0) scale(1);
  }
}

/*
 * Títulos com múltiplas linhas recebem uma cascata curta. O atraso da linha
 * respeita o atraso do próprio Reveal, impedindo que o texto apareça antes
 * do contêiner começar a entrar.
 */
.js .reveal .text-line {
  opacity: 0;
  transform: translate3d(0, 7px, 0);
  transition:
    opacity 820ms cubic-bezier(.45, 0, .25, 1),
    transform 980ms cubic-bezier(.45, 0, .25, 1);
}

.js .reveal--visible .text-line {
  opacity: 1;
  transform: translate3d(0, 0, 0);
}

.js .reveal .text-line:nth-child(1) { transition-delay: var(--reveal-delay); }
.js .reveal .text-line:nth-child(2) { transition-delay: calc(var(--reveal-delay) + 110ms); }
.js .reveal .text-line:nth-child(3) { transition-delay: calc(var(--reveal-delay) + 220ms); }
.js .reveal .text-line:nth-child(4) { transition-delay: calc(var(--reveal-delay) + 330ms); }

/* Textos internos de cartões permanecem estáveis; o cartão inteiro é animado. */
.credential-card .text-line,
.process-step .text-line,
.comfort-card .text-line,
.signal-item .text-line,
.final-points .text-line {
  opacity: 1 !important;
  transform: none !important;
  transition: none !important;
}

/* Estrelas surgem em sequência sem giro ou efeito de mola. */
.js .rating-stars svg {
  opacity: 0;
  transform: translate3d(0, 5px, 0) scale(.9);
}

.js .reveal--visible .rating-stars svg {
  animation: star-settle 760ms cubic-bezier(.45, 0, .25, 1) forwards;
  animation-delay: calc(var(--reveal-delay) + var(--star-delay, 0ms));
}

@keyframes star-settle {
  to {
    opacity: 1;
    transform: translate3d(0, 0, 0) scale(1);
  }
}

/* Números das etapas entram de forma sóbria, sem elasticidade. */
.js .process-step .step-number {
  opacity: 0;
  transform: translate3d(0, 7px, 0) scale(.9);
  transition:
    opacity 620ms cubic-bezier(.45, 0, .25, 1),
    transform 760ms cubic-bezier(.45, 0, .25, 1);
  transition-delay: calc(var(--reveal-delay) + 180ms);
}

.js .process-step.reveal--visible .step-number {
  opacity: 1;
  transform: translate3d(0, 0, 0) scale(1);
}

@media (max-width: 768px) {
  .reveal {
    --reveal-duration: 1080ms;
    --reveal-distance: 14px;
  }

  .hero-intro {
    animation-duration: 1120ms;
  }
}

@media (prefers-reduced-motion: reduce) {
  html {
    scroll-behavior: auto;
  }

  .js .reveal,
  .js .reveal--up,
  .js .reveal--left,
  .js .reveal--right,
  .js .reveal--scale,
  .js .reveal--fade {
    transform: none !important;
    transition: opacity 180ms linear !important;
    transition-delay: 0ms !important;
  }

  .js .reveal--visible {
    opacity: 1 !important;
    transform: none !important;
  }

  .hero-intro {
    transform: none !important;
    animation: hero-reduced-in 180ms linear forwards !important;
    animation-delay: 0ms !important;
  }

  .js .process-steps.reveal::before,
  .js .process-steps.reveal--visible::before {
    transform: scaleX(1) !important;
    transition: none !important;
  }

  .js .reveal .text-line,
  .js .rating-stars svg,
  .js .process-step .step-number {
    opacity: 1 !important;
    transform: none !important;
    animation: none !important;
    transition: none !important;
  }
}

@keyframes hero-reduced-in {
  to {
    opacity: 1;
  }
}

@media print {
  .reveal,
  .hero-intro {
    opacity: 1 !important;
    transform: none !important;
    filter: none !important;
    animation: none !important;
    transition: none !important;
  }
}

/*
 * Quando o título usa TextLines diretamente, as linhas controlam a entrada.
 * Isso evita somar o deslocamento do bloco ao deslocamento de cada linha.
 */
@supports selector(.reveal:has(> .text-line)) {
  .js .reveal:has(> .text-line) {
    opacity: 1;
    transform: none;
    transition: none;
  }
}

/*
 * Reveal e hover usam o mesmo transform. Após a entrada terminar, a classe
 * .reveal--settled devolve ao elemento a transição curta de interação.
 */
@media (hover: hover) and (pointer: fine) {
  .js .reveal--settled.benefit-item,
  .js .reveal--settled.credential-card,
  .js .reveal--settled.process-step {
    transition: transform .5s cubic-bezier(.45, 0, .25, 1);
  }

  .js .reveal--settled.comfort-card {
    transition:
      transform .55s cubic-bezier(.45, 0, .25, 1),
      box-shadow .55s cubic-bezier(.45, 0, .25, 1);
  }

  .js blockquote.reveal--settled {
    transition:
      transform .5s cubic-bezier(.45, 0, .25, 1),
      background .5s ease,
      border-color .5s ease;
  }

  .js .reveal--settled.benefit-item:hover,
  .js .reveal--settled.credential-card:hover,
  .js .reveal--settled.process-step:hover,
  .js .reveal--settled.comfort-card:hover,
  .js blockquote.reveal--settled:hover {
    transform: translate3d(0, -3px, 0) scale(1);
  }
}
